import { Channel, ExternalDocs, Info, JsonSchema, Message, Server, Tag } from "./types";
export interface IAsyncApiBuilderParams {
    info: Info;
    servers?: {
        [name: string]: Server;
    };
    defaultContentType?: string;
    tags?: Array<Tag>;
    externalDocs?: ExternalDocs;
}
export declare type MessageSchema = Message & {
    $id?: string;
};
export declare class AsyncApiBuilder {
    params: IAsyncApiBuilderParams;
    channels: Map<string, Channel>;
    messages: Map<string, Message>;
    schemas: Map<string, JsonSchema>;
    constructor(params: IAsyncApiBuilderParams);
    generate(): string;
    addChannel(name: string, pubMessages: MessageSchema[], subMessages: MessageSchema[], params?: any): void;
    private addMessageRef;
    private addSchemaRef;
}
